## Linked List Recipes
- ### **Clean** - Removes Build Output
    ```bash
    $ make

    or

    $ make clean
    ```

- ### **basic** - Creates a bst, checks if values are ordered correctly
    ```bash
    $ make basic
    ```